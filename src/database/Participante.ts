import $db from "./Database.js";
import Participante from "./model/Participante.js";

export class ParticipanteDAO {
  async insert(participante: Participante) {
    //    $db.prepare(`INSERT INTO participante ()`);
  }
}
